<% provide(:title, 'Inicio') %>
  <div class="center hero-unit">
    <h1>Inicio</h1>
    <h2>
      SSO Manager
      <a href="#">Software de Gestion Industrial</a>
      2013.
    </h2>

    <% if !signed_in? %>
      <%= link_to "Ingresar", signin_path, class: "btn btn-large btn-primary" %>
    <% end %>  
  </div>
